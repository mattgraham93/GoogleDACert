# here's an example of a variable

# assignment operator
first_var <- "This is my variable"
second_var <- 12.5

# vector, atomic = group of data elements of same type
vec_1 = c(12, 48.2, 81, 21, 42)
vec_1

# vector, list = group of data elements of more than one type
list_1 = list("a", 1L, 1.5, TRUE)
list_1

# pipe -> used to apply output from one function into another


# dataframes
data.frame(x = c(1, 2, 3) , y = c(1.5, 5.5, 7.5))